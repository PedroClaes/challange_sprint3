import React from 'react';
import { Navbar } from './Navbar';

export const Header: React.FC = () => {
    return (
        <header >
            <h1 >ConectaHC</h1>
            <Navbar />
        </header>
    );
};